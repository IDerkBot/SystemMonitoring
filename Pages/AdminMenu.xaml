<Page x:Class="SystemMonitoring.AdminMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700"
      VerticalAlignment="Center" HorizontalAlignment="Center"
      Title="Menu">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition Height="350"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Click="BtnMoveField_Click" Style="{StaticResource BtnMenu.Fields}"/>
        <Button Grid.Column="1" Grid.Row="0" Click="BtnMoveReports_Click" Style="{StaticResource BtnMenu.Reports}"/>
        <Button Grid.Column="0" Grid.Row="1" Click="BtnMoveCultures_Click" Style="{StaticResource BtnMenu.Base}"/>
        <Button Grid.Column="1" Grid.Row="1" Click="BtnMoveUsers_Click" Style="{StaticResource BtnMenu.User}"/>
    </Grid>
</Page>